# كيف تبني تطبيق أندرويد حقيقي 100% من جوالك (بدون كمبيوتر)

هذا المجلد يحوّل تطبيقك إلى APK حقيقي، كل الملفات محزومة بداخله،
بدون أي اعتماد على Netlify أو أي رابط خارجي.

البناء يصير على سيرفرات GitHub المجانية (GitHub Actions) — أنت بس
ترفع الملفات وتنتظر، وتنزّل الـ APK الجاهز.

---

## الخطوات

### 1. أنشئ حساب GitHub (لو ما عندك)
افتح github.com من متصفح جوالك وسجّل حساب مجاني.

### 2. أنشئ مستودع (Repository) جديد
- اضغط "+" أعلى الصفحة → "New repository"
- اسمه (مثلاً): hareeg-app
- خليه **Public** أو **Private** (الاثنين يشتغلوا مع GitHub Actions مجانًا)
- اضغط "Create repository"

### 3. ارفع كل ملفات هذا المجلد
- بصفحة المستودع، اضغط "Add file" → "Upload files"
- اسحب/ارفع **كل الملفات والمجلدات** اللي بهذا الأرشيف بالضبط بنفس البنية:
  - www/ (بكل ما بداخله)
  - .github/ (بكل ما بداخله — لازم يبقى مجلد .github بالحرف)
  - package.json
  - capacitor.config.json
  - icon.png
  - .gitignore

  **ملاحظة مهمة:** رفع الملفات عبر متصفح الجوال أحيانًا ما يدعم رفع
  مجلدات فرعية مباشرة. لو صار كذا، ارفع الأرشيف المضغوط (zip) نفسه
  كملف، ثم من داخل GitHub استخدم خيار "Upload files" ويدعم غالبًا
  فك الضغط تلقائيًا عند السحب، أو استخدم تطبيق GitHub الرسمي
  (متوفر لأندرويد) فيه دعم أفضل لرفع مجلدات كاملة.

- اكتب أي رسالة بخانة "Commit message" (مثلاً: "أول رفعة")
- اضغط "Commit changes"

### 4. تأكد إن البناء بدأ تلقائيًا
- روح لتبويب **"Actions"** أعلى صفحة المستودع
- المفروض تشوف عملية بناء شغّالة اسمها "Build Android APK"
  (تبدأ تلقائيًا فور الرفع)
- انتظر 5-10 دقايق (البناء ياخذ وقت أول مرة)

### 5. حمّل الـ APK
- بعد ما تخلص العملية (علامة ✅ خضراء)، اضغط عليها
- انزل لأسفل الصفحة لقسم **"Artifacts"**
- بتلاقي ملف اسمه **hareeg-app-debug** — اضغط عليه لتحميله
  (بينزل كملف zip يحتوي الـ APK بداخله)

### 6. ثبّت التطبيق
- فك ضغط الملف، ولاقي `app-debug.apk`
- افتحه على جوالك وثبّته (وافق على "تثبيت من مصادر غير معروفة" لو طلب)

---

## ✅ النتيجة
تطبيق حقيقي 100%، كل ملفاته (HTML/CSS/JS والأيقونات) محزومة
داخل الـ APK نفسه. حتى لو حذفت أو عطّلت Netlify بالكامل، أو ماكو
إنترنت إطلاقًا، التطبيق يفتح ويشتغل عادي — لأنه ما يحتاج يتصل
بأي سيرفر خارجي من الأساس.

## لتحديثه لاحقًا
عدّل الملفات بمجلد `www/` بنفس المستودع (من متصفح GitHub مباشرة
أو برفع ملفات جديدة فوق القديمة)، وGitHub Actions بيبني APK جديد
تلقائيًا في كل مرة ترفع تعديل.
